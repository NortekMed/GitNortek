---
description: Map the source project using semantic navigation when available
agent: code-navigator
---

Map the current GitNortek code project without modifying files. Prefer the
global Serena MCP for symbols, definitions, references, and call relationships
when connected. Report detected languages, entry points, important modules,
build and test commands found in the repository, and any uncertainty. Fall
back to normal read-only file inspection when Serena is unavailable.
